<% flash.each do |type, message| %>
  <% styles = type.to_sym == :notice ? "bg-green-50 text-green-800 border-green-200" : "bg-red-50 text-red-800 border-red-200" %>
  <div class="border px-4 py-3 text-sm text-center <%= styles %>" data-turbo-temporary>
    <%= message %>
  </div>
<% end %>
